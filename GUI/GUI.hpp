#pragma once

#include <glm/glm.hpp>

#include "../Renderer/Renderer.hpp"

#include <string>

class GUI {
public:
//    static bool Button(const glm::vec2& position, const glm::vec2& size, const glm::vec4 color, const std::string& text);
//    static bool Button(const glm::vec2& position, const glm::vec2& size, Ref<Texture> idle, Ref<Texture> hovered);
private:

};